export const environment = {
    production: false,
    // Backend local que já fala com o Supabase
    apiUrl: 'http://localhost:3000/api',
    // Defina estes valores e mude useSupabaseDirect para true se quiser pular o backend
    supabaseUrl: '',
    supabaseAnonKey: '',
    useSupabaseDirect: false
};
